[project]
name = "Fly-In"
version = "0.1.0"
description = "Fly in"
requires-python = ">=3.10"
dependencies = [
    "uv",
    "pydantic",
    "mypy",
    "pygame"
]

[tool.mypy]
ignore_missing_imports = true
plugins = ["pydantic.mypy"] 
